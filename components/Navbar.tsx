"use client"; import {Menu,X,ArrowUpRight} from 'lucide-react'; import {useState} from 'react';
export default function Navbar(){const [open,setOpen]=useState(false);const links=[['Services','/services'],['How we work','/how-we-work'],['Results','/case-studies'],['About','/about'],['Resources','/blog']];return <header className="sticky top-0 z-50 bg-[#f5f4ef]/90 backdrop-blur border-b border-black/10"><div className="wrap h-[76px] flex items-center justify-between"><a href="/" className="font-black text-2xl tracking-[-.08em]">ORBITLY<span className="text-lime">.</span></a><nav className="hidden lg:flex gap-7 text-sm font-semibold">{links.map(([x,h])=><a key={x} href={h} className="hover:opacity-50">{x}</a>)}</nav><a href="/contact" className="hidden md:inline-flex btn btn-dark">Let's talk <ArrowUpRight size={17}/></a><button className="md:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="md:hidden wrap py-5 flex flex-col gap-5 border-t border-black/10">{links.map(([x,h])=><a key={x} href={h} onClick={()=>setOpen(false)}>{x}</a>)}<a className="btn btn-dark justify-center" href="/contact">Let's talk</a></div>}</header>}
