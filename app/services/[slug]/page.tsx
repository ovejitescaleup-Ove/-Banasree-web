import {services} from '../../../lib/data'; import SiteShell from '../../../components/SiteShell'; import {ArrowRight,Check} from 'lucide-react'; import {notFound} from 'next/navigation';
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))} export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);return s?{title:s.seoTitle,description:s.seoDescription}:{} }
export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)return notFound();return <SiteShell><section className="gridline"><div className="wrap py-24 md:py-36"><p className="eyebrow">Service</p><h1 className="display text-6xl md:text-9xl font-black mt-5 max-w-6xl">{s.title.toUpperCase()}<span className="text-lime">.</span></h1><p className="text-xl md:text-2xl text-[#666] max-w-3xl mt-10 leading-relaxed">{s.description}</p></div></section><section className="wrap py-20 md:py-28 grid lg:grid-cols-2 gap-16"><div><p className="eyebrow">What we do</p><p className="text-lg text-[#666] leading-relaxed mt-5">{s.details}</p><a href="/contact" className="btn btn-dark mt-8">Talk about your goals <ArrowRight size={17}/></a></div><div><p className="eyebrow">Capabilities</p><div className="mt-6 space-y-4">{s.benefits.map(x=><div key={x} className="card p-5 font-bold flex gap-3"><Check className="text-lime bg-black rounded-full p-1" size={21}/>{x}</div>)}</div></div></section><section className="bg-[#111] text-white py-24"><div className="wrap"><p className="eyebrow text-white/50">Process</p><div className="grid md:grid-cols-4 gap-px bg-white/10 mt-10">{s.process.map((x,i)=><div key={x} className="bg-[#111] p-7"><span className="text-lime">0{i+1}</span><h2 className="text-2xl font-bold mt-10">{x}</h2></div>)}</div></div></section></SiteShell>}
