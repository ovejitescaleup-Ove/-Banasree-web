import {services} from '../../lib/data'; import SiteShell from '../../components/SiteShell'; import {ArrowUpRight} from 'lucide-react'; export const metadata={title:'Services | Orbitly',description:'Digital marketing services from Orbitly.'};
export default function Services(){return <SiteShell><section className="wrap py-24 md:py-36"><p className="eyebrow">Services</p><h1 className="display text-6xl md:text-9xl font-black mt-5 max-w-5xl">MARKETING SYSTEMS, NOT RANDOM TACTICS<span className="text-lime">.</span></h1><div className="grid md:grid-cols-2 gap-4 mt-20">{services.map((s,i)=><a href={'/services/'+s.slug} key={s.slug} className="card p-8 md:p-10 min-h-[300px] flex flex-col hover:-translate-y-1 transition"><span className="eyebrow">0{i+1}</span><h2 className="text-4xl font-black mt-10">{s.title}</h2><p className="text-[#666] mt-4 max-w-lg leading-relaxed">{s.description}</p><span className="mt-auto pt-8 font-bold">Explore <ArrowUpRight className="inline" size={17}/></span></a>)}</div></section></SiteShell>}
