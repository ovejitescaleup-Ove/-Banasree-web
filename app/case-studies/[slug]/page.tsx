import {cases} from '../../../lib/data'; import SiteShell from '../../../components/SiteShell'; import {notFound} from 'next/navigation'; export function generateStaticParams(){return cases.map(c=>({slug:c.slug}))} export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=cases.find(x=>x.slug===slug);return c?{title:c.seoTitle,description:c.seoDescription}:{} } export default async function Case({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=cases.find(x=>x.slug===slug);if(!c)return notFound();return <SiteShell><section className="bg-[#111] text-white py-24 md:py-36"><div className="wrap"><p className="eyebrow text-white/50">{c.industry}</p><h1 className="display text-6xl md:text-9xl font-black mt-5">{c.title.toUpperCase()}<span className="text-lime">.</span></h1></div></section><section className="wrap py-20 md:py-28 grid lg:grid-cols-3 gap-12"><div><p className="eyebrow">Challenge</p><p className="text-[#666] leading-relaxed mt-5">{c.challenge}</p></div><div><p className="eyebrow">Strategy</p><p className="text-[#666] leading-relaxed mt-5">{c.strategy}</p></div><div><p className="eyebrow">Results</p><div className="mt-5 space-y-3">{c.results.map(r=><div className="card p-5 font-bold" key={r}>{r}</div>)}</div></div></section></SiteShell>}
