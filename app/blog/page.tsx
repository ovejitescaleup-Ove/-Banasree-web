import SiteShell from '../../components/SiteShell'; import {posts} from '../../lib/data'; export const metadata={title:'Resources | Orbitly',description:'Digital marketing guides and insights from Orbitly.'}; export default function Blog(){return <SiteShell><section className="wrap py-24 md:py-36"><p className="eyebrow">Resources</p><h1 className="display text-6xl md:text-9xl font-black mt-5">USEFUL IDEAS FOR <span className="text-lime">GROWTH.</span></h1><div className="grid md:grid-cols-2 gap-4 mt-20">{posts.map(p=><a key={p.slug} href={'/blog/'+p.slug} className="card p-8 min-h-[300px] flex flex-col"><span className="eyebrow">{p.category} · {p.date}</span><h2 className="text-3xl font-black mt-12">{p.title}</h2><p className="text-[#666] mt-4">{p.excerpt}</p><span className="mt-auto pt-8 font-bold">Read article →</span></a>)}</div></section></SiteShell>}
