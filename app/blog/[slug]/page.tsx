import {posts} from '../../../lib/data'; import SiteShell from '../../../components/SiteShell'; import {notFound} from 'next/navigation'; export function generateStaticParams(){return posts.map(p=>({slug:p.slug}))} export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=posts.find(x=>x.slug===slug);return p?{title:p.title+' | Orbitly',description:p.excerpt}:{} } export default async function Post({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=posts.find(x=>x.slug===slug);if(!p)return notFound();return <SiteShell><article className="wrap py-24 md:py-36 max-w-4xl"><p className="eyebrow">{p.category} · {p.date}</p><h1 className="display text-6xl md:text-8xl font-black mt-5">{p.title}<span className="text-lime">.</span></h1><div className="prose-orbitly mt-14"><p>{p.content}</p><h2>Make decisions from evidence</h2><p>Use clean measurement, focused testing and regular reviews to understand what is actually moving the business forward. The right next step is usually clearer once the data and customer journey are viewed together.</p></div></article></SiteShell>}
